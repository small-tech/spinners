# @small-tech/spinners Svelte example

## Run

1. Install dependencies.

    ```shell
    npm install
    ```

2. Run the SvelteKit app.

    ```shell
    npm run dev
    ```

3. Open _http://localhost:3000_ in your browser.

